---
apiVersion: v1
kind: Service
metadata:
  namespace: kafka
  name: kafka-service
  labels:
    app: kafka
spec:
  ports:
    - name: bootstrap
      port: 9092
      protocol: TCP
    - name: metrics
      port: 5556

  ipFamilyPolicy: PreferDualStack
  selector:
    app: kafka

