apiVersion: v1
kind: Service
metadata:
  namespace: kafka
  name: zookeeper-service
  labels:
    app: zookeeper
spec:
  ports:
    - name: client
      port: 2181
      protocol: TCP
  ipFamilyPolicy: PreferDualStack
  selector:
    app: zookeeper
