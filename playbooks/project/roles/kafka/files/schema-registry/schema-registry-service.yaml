---
apiVersion: v1
kind: Service
metadata:
  name: schema-registry-service
  namespace: default
spec:
  selector:
    app: schema-registry
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
  clusterIP: None
